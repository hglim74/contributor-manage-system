# Donor Display Management System - Backend

이 프로젝트는 기부자 정보를 관리하고 Unity 클라이언트와 실시간으로 통신하는 Backend 시스템입니다. FastAPI를 기반으로 작성되었습니다.

## 📁 파일 생성 가이드 (File Creation Guide)

프로젝트 실행에 필요한 주요 파일들에 대한 설명입니다.

### 1. `requirements.txt`
Python 패키지 의존성을 관리하는 파일입니다. 소스코드 분석을 통해 필요한 라이브러리를 이미 포함하여 생성해두었습니다.
- **포함된 패키지**: `fastapi`, `uvicorn`, `sqlalchemy`, `pandas`, `python-multipart`, `pydantic`

### 2. `.gitignore`
Git 버전 관리에서 제외할 파일들을 지정합니다.
- 가상환경 폴더(`venv/`, `env/`), 파이썬 캐시(`__pycache__/`), 로컬 데이터베이스(`donors.db`) 등이 제외됩니다.

### 3. `bulk_upload_template.csv`
대량 기부자 등록 기능을 테스트하기 위한 샘플 CSV 파일입니다.
- **포맷**: `name,amount,grade,message`
- **예시**:
  ```csv
  name,amount,grade,message
  홍길동,100000,GOLD,화이팅
  ```

### 4. `donors.db` (자동 생성)
SQLite 데이터베이스 파일입니다.
- **생성 시점**: 서버(`main.py`)가 처음 실행될 때 자동으로 생성됩니다. (`models.Base.metadata.create_all` 호출 시)
- **주의**: 이 파일은 Git에 포함되지 않도록 `.gitignore`에 등록되어 있습니다.

---

## 🚀 실행 가이드 (Execution Guide)

### 1. 환경 설정 (Prerequisites)
- Python 3.8 이상이 설치되어 있어야 합니다.

### 2. 패키지 설치 (Installation)
터미널에서 `backend` 디렉토리로 이동한 후, 다음 명령어를 실행하여 의존성 패키지를 설치합니다.

```bash
pip install -r requirements.txt
```

### 3. 서버 실행 (Run Server)
다음 두 가지 방법 중 하나로 서버를 실행할 수 있습니다.

**방법 A: 직접 실행**
```bash
python main.py
```

**방법 B: Uvicorn으로 실행 (개발 모드)**
```bash
uvicorn main:app --reload
```

### 4. 기능 확인 (Verification)
서버가 정상적으로 실행되면 다음 주소에 접속하여 기능을 확인할 수 있습니다.

- **API 문서 (Swagger UI)**: [http://localhost:8000/docs](http://localhost:8000/docs)
  - API 명세를 확인하고 직접 테스트해볼 수 있습니다.
- **Admin 페이지**: [http://localhost:8000/static/admin.html](http://localhost:8000/static/admin.html)
  - 기부자 관리 및 CSV 업로드 기능을 테스트할 수 있는 프론트엔드 페이지입니다.

### 5. Unity 클라이언트 (Unity Client)
Unity 클라이언트 실행 및 설정 방법은 `frontend_unity/README.md` 파일을 참고하세요.

